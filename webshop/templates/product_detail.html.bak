{% extends 'base.html' %}
{% load static %}




{% block content %}

<section class="section-content padding-y bg">
    <div class="container">
    
    <!-- ============================ COMPONENT 1 ================================= -->
    <div class="card">
        <div class="row no-gutters">
            <aside class="col-md-6">
    <article class="gallery-wrap"> 
        <div class="img-big-wrap">
           <a href="#"><img src={% static product.product_image_path %}></a>
        </div> <!-- img-big-wrap.// -->
        
    </article> <!-- gallery-wrap .end// -->
            </aside>
            <main class="col-md-6 border-left">
    <article class="content-body">
    
    <h2 class="title">{{ product.product_name }}</h2>
    
    <div class="mb-3"> 
        <var class="price h4">{{ product.price|floatformat:2 }}€</var> 
    </div> 
    
    <p>{{ product.product_description }}</p>


        <div class="mb-3" style="width:fit-content !important">
            <br>
            <form action={% url 'like' pkProduct=product.Product_ID pkCustomer=User.id %} style="grid-row: auto;">
                <button class="btn btn-light" type="submit" id="button-like" onclick="like();">
                    <p style="color: #2de7de;" id="button-like-text">
                        {% if likes > 0 %}
                        &#9825; {{ likes }}
                        {% else %}
                        &#9829; {{ likes }}
                        {% endif %}
                    </p>
                </button>
                <p>Gefällt dir unser Produkt?</p>
                <p>{{ they_are_watching_us }}</p>
            </form>
        </div>

    <div class="mb-3" style="float: right;">
        <br>
        <br>
        <br>
        <br>
        <br>
        <form action="/webshop/shop/views.py">
            <!-- col.// -->
            <div class="col"> 
                <div class="input-group input-spinner">
                    <div class="input-group-prepend">
                    <button class="btn btn-light" type="button" id="button-plus" onclick="minus();"> <i class="fa fa-minus"></i> </button>
                    </div>
                    <input type="text" id="quantity" class="form-control"  value="1">
                    <div class="input-group-append">
                    <button class="btn btn-light" type="button" id="button-minus" onclick="plus();"> <i class="fa fa-plus"></i> </button>
                    </div>
                </div> <!-- input-group.// -->
                <a href="./product-detail.html" class="btn  btn-primary"> 
                    <span class="text">Add to cart</span> <i class="fas fa-shopping-cart"></i>  
                </a>
            </div> <!-- col.// -->
        </form> <!-- search-wrap .end// -->
    </div>


    </article> <!-- product-info-aside .// -->
            </main> <!-- col.// -->
        </div> <!-- row.// -->
    </div> <!-- card.// -->
    <!-- ============================ COMPONENT 1 END .// ================================= -->
    

{% endblock %}